// Cycled by a class's index in plan.classes — shared so the same class shows
// the same color on both the Dashboard and Planner.
export const COURSE_COLORS = ['#3B82F6', '#DB6B2C', '#1D9E75', '#9333EA', '#DC2626', '#0284C7'];

// Light-tint backgrounds paired index-for-index with COURSE_COLORS, reusing
// the app's existing soft-color tokens where they already match.
export const COURSE_SOFT_COLORS = ['#E8EEFC', '#FBEAE0', '#E3F5EE', '#F1E7FB', '#FEF2F2', '#E0F2FE'];
